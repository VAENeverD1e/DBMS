# .env.development (in your Vite folder)

# VITE_ prefix is required for client-side access in Vite
# This points the frontend to your running Flask backend
VITE_API_BASE_URL=http://localhost:5000/api